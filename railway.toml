[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["cd Server && dotnet restore && dotnet publish -c Release -o out"]

[start]
cmd = "cd Server/out && dotnet Server.dll"